#!/usr/bin/env bash

set -euo pipefail

input="$1"
output="${input%.*}.mp3"

ffmpeg \
    -ss 1 \
    -i "$input" \
    -vn \
    -c:a libmp3lame \
    -q:a 2 \
    "$output"

echo "Created: $output"

